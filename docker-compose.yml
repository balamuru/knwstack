services:
  nats:
    image: nats:latest
    container_name: knwstack-nats
    ports:
      - "4222:4222" # Client port
      - "8222:8222" # Monitoring port
    command: "-js -m 8222" # Enable JetStream and Monitoring
    volumes:
      - nats_data:/data
    restart: always

volumes:
  nats_data:
